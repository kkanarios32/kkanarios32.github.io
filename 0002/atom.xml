<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>Kellen Kanarios</name>
    <uri>https://kellenkanarios.com/kellenkanarios/</uri>
  </author>
  <updated>2026-06-24T21:42:52Z</updated>
  <title>Blog</title>
  <id>https://kellenkanarios.com/0002/</id>
  <link rel="alternate" href="https://kellenkanarios.com/0002/" />
  <link rel="self" href="https://kellenkanarios.com/0002/atom.xml" />
  <entry>
    <title>My Notes on Productivity</title>
    <published>2026-06-24T21:42:52Z</published>
    <updated>2026-06-24T21:42:52Z</updated>
    <author>
      <name>Kellen Kanarios</name>
      <uri>https://kellenkanarios.com/kellenkanarios/</uri>
    </author>
    <link rel="alternate" type="text/html" href="https://kellenkanarios.com/7BUX/" />
    <id>https://kellenkanarios.com/7BUX/</id>
    <content type="xhtml">
      <div xmlns="http://www.w3.org/1999/xhtml">
<p>Due to the unstructured nature of PhD life / knowledge work in general, I have been trying to come up with the most effective system to ensure I am <em>Getting Things Done</em> (ironically I found this system actually not that helpful).</p><p>Since <a href="https://kellenkanarios.com/calnewport/">Cal Newport</a> is a professor (can relate to the knowledge work problem), I have been reading a lot of his stuff on productivity.</p><ul><li>Deep work.</li>
  <li>Slow productivity.</li></ul><p>I often refer back to Atomic Habits as it intuitively just makes so much sense to me.</p><ul><li><em>Start small</em>: starting a habit should take less than 2 minutes.</li>
  <li><em>Never miss a habit twice</em>.</li>
  <li><em>Habit anchors</em>: I am always going to run so now after I run I stretch, which has resulted in remarkable stretching consistency (see my <a href="https://kellenkanarios.com/007S/">Weeknotes</a>).</li></ul><section><header><h2>My Current Workflow (constantly changing)</h2></header><p>
I have been using <a href="https://kellenkanarios.com/todoist.com">todoist</a> and <a href="https://kellenkanarios.com/habitify.me">habitify</a>. I decided on these tools primarily because of their API availability. I wanted to be able to add / complete / see outstanding tasks or habits for the day without the allure of everything else my browser has to offer. Also visualization for my <a href="https://kellenkanarios.com/007S/">Weeknotes</a>.
</p><ul><li>I had Claude write a little script to have my next tasks listed when clicked in my status bar, so I do not have to open the browser to check what I need to do today.</li></ul><p>
It seems how to properly use these (todoist in particular) is the subject of much debate. Here, I will outline what I converged to / works for ME right now. I have broken down my system into a hierarchy:
</p><ol><li>Higher level: how to actually choose what to do.</li>
  <li>Lower level: how to actually do the chosen task.</li></ol>
  <section><header><h3>Figuring out what to do</h3></header>
  <p>
    This is effectively a task management system.
  </p>
  <p><strong style="font-family: var(--sans);">Work streams.</strong> I took this name from my investment banker friends (sounds cool / important). Basically, these are groupings I have made and allocated time for each day. They roughly consist of the following:
  </p>
    <ul><li>Research / work (4hrs first thing in the morning):</li>
      <ul><li>Active work towards new artifact or paper i.e. coding up ideas.</li>
        <li>In the absence of ideas, studying existing relevant papers.</li></ul>
      <li>Deliberate practice (2hrs after run / lunch):</li>
      <ul><li>Name taken from <a href="https://kellenkanarios.com/calnewport/">Cal Newport</a>.</li>
        <li>As of writing this, a current example is spending ~2hrs a day writing / optimizing known kernels in CuTe / Pallas / NKI.</li>
        <li>Basically, something I need to understand or master for research that is not novel.</li></ul>
      <li>Extraneous Study (30min - 1hr):</li>
      <ul><li>Things not directly related to research, but I am curious about.</li>
        <li>I try to maintain study of related computer science fields. Primarily, studying a textbook in something like <a href="https://kellenkanarios.com/0084/">OS</a>, <a href="https://kellenkanarios.com/P3GB/">computer architecture</a>, etc.</li></ul>
      <li>Habits / daily upkeep (1hr normally end of day):</li>
      <ul><li>Mainly consists of reviewing Anki cards daily that come from extraneous study / deliberate practice.</li></ul></ul>
  </section>
<p>
To decide on the next task, I use a priority-based system:
</p><ol><li>Things I will be working on today / until they are done. At most <strong>ONE</strong> thing per work stream.</li>
  <li>The <em>backlog</em> of things to do next</li>
  <ul><li>An index to find things to move to priority 1 for the next day</li> 
    <li>Keep relatively small to avoid the crippling nature of choice - roughly on the order of things I want done this week.</li></ul>
  <li>A larger backlog of things - should be completed in the next month or so.</li>
  <li>Things I am not working on right now, or have any need to be completed any time soon.</li></ol><p>
This idea is mainly based on the <a href="https://kellenkanarios.com/4WG5/">MLFQ</a>, which has been proven effective for balancing response / turnaround time. To maintain this system, I have reviews each day and week (tracking with <a href="https://kellenkanarios.com/habitify.me">habitify</a>) to make sure I stay consistent.
</p>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(0, 100, 255, 0.7);
  border-radius: var(--radius);
  background-color: rgba(0, 100, 255, 0.05);
  "><span style="font-weight: 600; color: rgba(0, 100, 255, 0.7);">Algorithm.</span> 
  <em>Weekly review.</em>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(0, 100, 255, 0.7);
  border-radius: var(--radius);
  background-color: rgba(0, 100, 255, 0.05);
  "><span style="font-weight: 600; color: rgba(0, 100, 255, 0.7);">Algorithm.</span> 
  <em>Daily review.</em>
  <ul><li>For each work stream,</li>
    <ul><li>If I finished the allocated task for the day, get new task from priority 2.</li>
     <li>Else, do nothing and continue with the current task.</li></ul>
    <li>Review inbox and move new tasks to corresponding work stream for review on Friday.</li>
    <ul><li>If immediately important, can move directly to priority 1 / 2.</li></ul>
    <li>Look at calendar for next day and timeblock work streams for the next day.</li>
    <ul><li>Since PhD work is so hard to predict, I have found timeblocking any further than one day in advance to be a futile effort.</li></ul></ul>
  </div>

  <ul><li>Typically done on Fridays, so that I can dynamically allocate time on weekends if needed.</li>
    <li>For each work stream,</li>
    <ol><li>Look in remaining priority 2 and priority 3 tasks.</li>
     <li>Define what I think a successful week would look like (e.g. if I get all these things done next week I will be happy).</li>
     <ul><li>Look at calendar for week to guide this decision.</li></ul>
     <li>Get rid of at least two of those tasks (initial plan always too ambitious).</li>
     <li>Move / keep remaining tasks in priority 2.</li></ol>
    <li>Write <a href="https://kellenkanarios.com/007S/">Weeknotes</a> for the week.</li></ul>
</div>

  <section><header><h3>What to do when actually working on a task</h3></header>
  <p>
This is the harder part (and requires the most discipline). When working on a task, especially a more open-ended one like research, it is very common that a million other things come up that seem immediately important. All of the sudden, you have a great idea for the research project you haven't worked on in months. 
  </p>
  <p>
  It is <strong>crucial</strong> to collect and forget about these things until they are scheduled. This is the one thing I did take from <a href="https://gettingthingsdone.com/"><em>Getting Things Done</em></a>. The system should <em>flow like water</em>. Making this achievable is where the todoist API becomes crucial.</p>
  <ul><li>I have a script bound by dwm that pops up vim for me to write down the task and, on save, it sends it to my inbox in todoist.</li>
    <li>I have more involved ones that allow me to actually allocate it to the specific work stream / priority if it is immediately obvious.</li></ul>
  <p>
  Another thing that I have found useful to actually achieving this watery flow is the <a href="https://en.wikipedia.org/wiki/Pomodoro_Technique">pomodoro technique</a>. This basically lets me gaslight myself into thinking I can go to the other things as soon as the timer ends (which is intentionally always just close enough). By the end, I often no longer have this urge and can continue on the scheduled tasks.
</p>
</section>
</section>
  <div class="comments">
    
  <hr />

    <script src="https://utteranc.es/client.js" repo="kkanarios32/website-comments" issue-term="pathname" theme="boxy-light" crossorigin="anonymous" async="" />
  </div>
      </div>
    </content>
  </entry>
  <entry>
    <title>Flash Attention (v1)</title>
    <published>2026-05-25T11:48:03Z</published>
    <updated>2026-05-25T11:48:03Z</updated>
    <author>
      <name>Kellen Kanarios</name>
      <uri>https://kellenkanarios.com/kellenkanarios/</uri>
    </author>
    <link rel="alternate" type="text/html" href="https://kellenkanarios.com/R0PM/" />
    <id>https://kellenkanarios.com/R0PM/</id>
    <content type="xhtml">
      <div xmlns="http://www.w3.org/1999/xhtml">
<section><header><h2>Online Softmax</h2></header><p>This covers the math behind the online softmax algorithm required by <a href="https://kellenkanarios.com/R0PM/">Flash Attention (v1)</a>.</p>
  <section><header><h3>Safe softmax</h3></header>
<p>The first thing we must consider is numerical precision. Namely, the <a href="https://kellenkanarios.com/C00V/">original softmax</a> operation is given by</p>
<code>\mathrm {softmax}(\mathbf {x}) = \frac {\exp (\mathbf {x})}{\sum _{i=1}^n \exp (\mathbf {x}_i)}</code>
<p>However, when operating with <a href="https://kellenkanarios.com/DEQV/">low precision data types</a> (i.e. float16) the maximum value is <code>65536</code>. Therefore, with <code>\exp </code> we can easily get numerically overflow.</p>
<p>To get around this, the online softmax algorithm adheres to the <em>safe softmax</em>. For safe softmax, we simply multiply the numerator and denominator by <code>\exp (-m)</code>, where <code>m = \max  \{x_{1}, \ldots , x_n\}</code>. This gives</p>
<code>\mathrm {softmax}(\mathbf {x}) = \frac {\exp (\mathbf {x} - m)}{\sum _{i=1}^n \exp (\mathbf {x}_i - m)}</code>
<p>Since <code>x_i - m \leq  0</code>, this ensures that each <code>\exp (x_i - m)</code> is bounded in a safe range.</p>


 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(200, 55, 45, 0.85);
  border-radius: var(--radius);
  background-color: rgba(200, 55, 45, 0.06);
  "><span style="font-weight: 600; color: rgba(200, 55, 45, 0.85);">Problem.</span> Note that computing the <code>\max </code> over <code>\mathbf {x}</code> means that we must have all of the logits in a row of <code>QK^T</code> materialized in <a href="https://kellenkanarios.com/8GZN/">SMEM</a>, or first loop over all of them to compute the max before computing the normalization constant.</div>

</section>

  <section><header><h3>Fusing the max and normalization</h3></header>
<p>The online softmax algorithm fuses the max reduction into the normalization pass, so a single sweep over the row suffices to compute the normalization with the max. The key difficulty is that the denominator depends on <code>n</code> <strong>for each</strong> <code>i</code> i.e.</p>
<code>d = \sum _{i=1}^{n} \exp (x_i - m_n)</code>
<p>
Therefore, we must find a recursive relationship such that <code>d_{i+1} = f(d_i, m_i, x_{i+1})</code> for some function <code>f</code>. This would then allow us to locally compute <code>d_{i+1}</code> with only the local information of current <code>x_{i+1}</code> and the two running statistics <code>d_i</code> and <code>m_i</code>. This is exactly the kind of algorithm that is conducive to <a href="https://kellenkanarios.com/OGCRV/">memory tiling</a>.
</p>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(200, 130, 0, 0.9);
  border-radius: var(--radius);
  background-color: rgba(200, 130, 0, 0.06);
  "><span style="font-weight: 600; color: rgba(200, 130, 0, 0.9);">Question.</span> What is <code>f(d_i, m_i, x_{i+1})</code>?</div>


 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(0, 140, 120, 0.9);
  border-radius: var(--radius);
  background-color: rgba(0, 140, 120, 0.06);
  "><span style="font-weight: 600; color: rgba(0, 140, 120, 0.9);">Answer.</span> Consider <code>d_{i+1} = d_i \cdot  \exp (m_{i} - m_{i + 1}) + \exp (x_{i + 1} - m_{i + 1})</code>.</div>


 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(0, 0, 0, 0.4);
  border-radius: var(--radius);
  background-color: rgba(0, 0, 0, 0.03);
  "><span style="font-weight: 600; color: rgba(0, 0, 0, 0.4);">Proof.</span> 
  This can be seen by directly rewriting <code>d_{i + 1}</code>. 
  <code>
    \begin {align*}
      d_{i + 1} &amp;= \sum _{j = 1}^{i+1} \exp (x_j - m_{i + 1}) \\
      &amp;= \left (\sum _{j = 1}^{i} \exp (x_j - m_{i+1})\right ) + \exp (x_{i+1} - m_{i+1}) \\
      &amp;= \left (\sum _{j = 1}^{i} \exp (x_j - m_{i})\right )\exp (m_{i} - m_{i + 1}) + \exp (x_{i+1} - m_{i+1}) \\
      &amp;= d_{i} \exp (m_{i} - m_{i + 1}) + \exp (x_{i+1} - m_{i+1})
    \end {align*}
  </code>
</div>

<p>
The running denominator <code>d</code> is rescaled by <code>e^{m - m_{\text {new}}}</code> each time a new maximum is found, keeping it consistent with the safe-softmax shift.</p>
<p><img src="https://kellenkanarios.com/4a2d5b3202e83ff6780ef6fedca07f67.svg" /></p>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(90, 90, 110, 0.7);
  border-radius: var(--radius);
  background-color: rgba(90, 90, 110, 0.05);
  "><span style="font-weight: 600; color: rgba(90, 90, 110, 0.7);">Remark.</span> 
Unfortunately, we cannot get around the second pass to compute the normalized output for <code>\mathbf {p}</code>. However, this is not the case in <a href="https://kellenkanarios.com/R0PM/">Flash Attention (v1)</a>.
</div>

</section>
</section><section><header><h2>Flash Attention (Fwd)</h2></header><p>
Unlike in the <a href="https://kellenkanarios.com/PI7N/">online softmax</a>, we are not interested in the outputted probability vector <code>\mathbf {p}</code>. Instead we are interested in the entire output matrix <code>O</code> given by
  </p><code>O = \mathrm {softmax}(QK^T) V</code><p>
For more details see <a href="https://kellenkanarios.com/004G/">self-attention</a>.
  </p>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(70, 100, 150, 0.75);
  border-radius: var(--radius);
  background-color: rgba(70, 100, 150, 0.05);
  "><span style="font-weight: 600; color: rgba(70, 100, 150, 0.75);">Notation.</span> 
  In correspondence with the notation in the <a href="https://kellenkanarios.com/PI7N/">online softmax</a>, we define the following:
  <code>x_{i,j} = \mathbf {q}_i \cdot  \mathbf {k}_j,</code>
  where <code>\mathbf {q}_i</code> is the <code>i</code>th row of <code>Q</code> and <code>\mathbf {k}_j</code> is the <code>j</code>th row of <code>K</code>. Similarly, <code>\mathbf {o}_i</code> is the <code>i</code>th row of the output matrix <code>O</code> given by
  <code>\mathbf {o}_i = \sum _{j = 1}^{n} \frac {\exp (x_{i,j} - m_n)}{d_n} \cdot  \mathbf {v}_j </code>
</div>
<p>Now following the template provided by the <a href="https://kellenkanarios.com/PI7N/">online softmax</a>, we must find a recurrence for <code>\mathbf {o}_i</code>. Namely, we will consider the sequence <code>\mathbf {o}_{i,k}'</code> given by
<code>\mathbf {o}_{i,k}' = \sum _{j = 1}^{k} \frac {\exp (x_{i,j} - m_k)}{d_k} \cdot  \mathbf {v}_j, </code>
such that <code>\mathbf {o}_{i,n}' = \mathbf {o}_n</code></p>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(140, 60, 170, 0.8);
  border-radius: var(--radius);
  background-color: rgba(140, 60, 170, 0.05);
  "><span style="font-weight: 600; color: rgba(140, 60, 170, 0.8);">Claim.</span> 
  We have the following recurrence for <code>\mathbf {o}_{i,k}'</code>:
  <code>
  \mathbf {o}_{i,k}' = \mathbf {o}_{i, k-1} \cdot  \frac {d_{k - 1} \cdot  \exp (m_{k-1} - m_{k})}{d_k} + \frac {\exp (x_{i,j} - m_k)}{d_k} \cdot  \mathbf {v}_k
    </code>
</div>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(0, 0, 0, 0.4);
  border-radius: var(--radius);
  background-color: rgba(0, 0, 0, 0.03);
  "><span style="font-weight: 600; color: rgba(0, 0, 0, 0.4);">Proof.</span> 
  We will proceed by directly rewriting the recurrence for <code>\mathbf {o}_{i,k}'</code> as 
  <code>
    \begin {align*}
      \mathbf {o}_{i,k}' &amp;= \sum _{j = 1}^{k} \frac {\exp (x_{i,j} - m_k)}{d_k} \cdot  \mathbf {v}_j \\
                        &amp;= \left (\sum _{j = 1}^{k - 1} \frac {\exp (x_{i,j} - m_{k-1})}{d_{k-1}} \cdot  \mathbf {v}_j\right ) \frac {d_{k - 1} \cdot  \exp (m_{k-1} - m_{k})}{d_k} +  \frac {\exp (x_{i,k} - m_k)}{d_k} \cdot  \mathbf {v}_k \\
                        &amp;= \mathbf {o}_{i, k-1} \cdot  \frac {d_{k - 1} \cdot  \exp (m_{k-1} - m_{k})}{d_k} +  \frac {\exp (x_{i,k} - m_k)}{d_k} \cdot  \mathbf {v}_k
    .\end {align*}
    </code>
  </div>
<p>Thus, we are left with the following algorithm</p><p><img src="https://kellenkanarios.com/e443552973fe32fe694e594b454313d2.svg" /></p>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(90, 90, 110, 0.7);
  border-radius: var(--radius);
  background-color: rgba(90, 90, 110, 0.05);
  "><span style="font-weight: 600; color: rgba(90, 90, 110, 0.7);">Remark.</span> 
A key benefit of this algorithm is that it is entirely parallelizable across rows. This means that we can loop across entire <a href="https://kellenkanarios.com/OGCRV/">tiles</a> like in the GEMM case.
</div>
</section><section><header><h2>Flash Attention (Bwd)</h2></header><p>To understand the innovation of the backward pass in <a href="https://kellenkanarios.com/daoFlashAttentionFastMemoryEfficient2022/">flash attention</a>, we must first find all of the gradients we must compute.</p>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(70, 100, 150, 0.75);
  border-radius: var(--radius);
  background-color: rgba(70, 100, 150, 0.05);
  "><span style="font-weight: 600; color: rgba(70, 100, 150, 0.75);">Notation.</span> 
Following the notation in the <a href="https://kellenkanarios.com/daoFlashAttentionFastMemoryEfficient2022/">original paper</a>, we have the following matrices
<code>\mathbf {S} = \mathbf {Q} \mathbf {K}^T, \quad  \mathbf {P} = \mathrm {softmax}(\mathbf {S}), \quad  \mathbf {O} = \mathbf {P} \mathbf {V}</code>
Now suppose their is some scalar loss function <code>\phi </code>. Then we define 
<code>\frac {\partial  \phi }{\partial  \mathbf {O}}, \frac {\partial  \phi }{\partial  \mathbf {K}}, \frac {\partial  \phi }{\partial  \mathbf {V}}  = d\mathbf {O}, d\mathbf {K}, d\mathbf {V}</code>
</div>
<p>We will compute each gradient individually.</p><ol><li><code>d\mathbf {P}</code>: this is just a straightforward application of the chain rule. In particular, <code>\mathbf {O} = \mathbf {P} \mathbf {V}</code> so that
  <code>
     d\mathbf {P} = \frac {\partial  \phi (\mathbf {O})}{\partial  \mathbf {P}} = \frac {\partial  \phi (\mathbf {O})}{\partial  \mathbf {O}} \frac {\partial  \mathbf {O}}{\partial  \mathbf {P}} = d\mathbf {O} \mathbf {V}^T
    </code>
    <p>
    To sanity check, let us look at the shapes. <code>d\mathbf {O}</code> is the partial with respect to the outputs <code>O</code>, so we need an entry for each output and it is <code>N \times  d</code>. <code>V^T</code> is <code>d \times  N</code>, so that the result <code>d\mathbf {P}</code> is <code>N \times  N</code> as desired.
    </p>
    <p>Furthermore, we can see this by writing <code>\mathbf {O}_{ik} = \sum _{j} \mathbf {P}_{ij} \mathbf {V}_{jk}</code>. Then</p>
    <code>
      \begin {align*}
       d\mathbf {P}_{ij} &amp;= \sum _{i} \frac {\partial  \phi }{\partial  \mathbf {O}_{ik}} \cdot  \frac {\partial  \mathbf {O}_{ik}}{\partial  \mathbf {P}_{ij}}  \\
                        &amp;= \sum _{i} d\mathbf {O}_{ik} \mathbf {V}_{jk} 
      .\end {align*}
    </code></li>
  <li><code>d\mathbf {V}</code>: this is just the same as <code>d\mathbf {P}</code>. In particular, <code>\mathbf {O} = \mathbf {P} \mathbf {V}</code> so that
  <code>
     d\mathbf {V} = \frac {\partial  \phi (\mathbf {O})}{\partial  \mathbf {V}} = \frac {\partial  \phi (\mathbf {O})}{\partial  \mathbf {O}} \frac {\partial  \mathbf {O}}{\partial  \mathbf {V}} = \mathbf {P}^T d\mathbf {O} 
    </code>
    <p>Furthermore, we can see this by writing <code>\mathbf {O}_{ik} = \sum _{j} \mathbf {P}_{ij} \mathbf {V}_{jk}</code>. Then</p>
    <code>
      \begin {align*}
       d\mathbf {V}_{jk} &amp;= \sum _{i} \frac {\partial  \phi }{\partial  \mathbf {O}_{ik}} \cdot  \frac {\partial  \mathbf {O}_{ik}}{\partial  \mathbf {V}_{jk}}  \\
                        &amp;= \sum _{i} d\mathbf {O}_{ik} \mathbf {P}_{ij} \\
                        &amp;= \sum _{i} \frac {\exp  \left (\mathbf {Q}_{i} \cdot  \mathbf {K}_{j}\right )}{\mathbf {L}_{i}} d\mathbf {O}_{ik}
      .\end {align*}
    </code></li>
  <li><code>d\mathbf {S}</code>: now we apply chain rule with <code>d\mathbf {P}</code>. However, we must take the gradient through the <a href="https://kellenkanarios.com/C00V/">softmax</a>. Exiting matrix notation hell, if we consider the scalar case. Let <code>\mathbf {s}</code> and <code>\mathbf {p}</code> just be rows. Then using the <a href="https://kellenkanarios.com/C00V/">scalar softmax derivation</a>
    <code>
      \begin {align*}
       d\mathbf {s}_j &amp;= \sum _{i} d\mathbf {p}_i \cdot  \frac {\partial  \mathbf {p}_i}{\partial  \mathbf {s}_j} \\  
       &amp;= \sum _{i} d\mathbf {p}_i \cdot  (\delta _{ij}\mathbf {p}_i - \mathbf {p}_{ij}) \\
       &amp;= d \mathbf {p}_{j} \mathbf {p}_{j} - \mathbf {p}_{j}\sum _{i} d\mathbf {p}_{i} \mathbf {p}_{i}
      .\end {align*}
      </code>
    Now if we rewrite this as the row vector, we get
    <code>
      \begin {align*}
        d\mathbf {S}_{k} &amp;= \mathbf {P}_{k} \odot  (d\mathbf {P}_{k} - \mathbf {P}_k \cdot  d\mathbf {P}_k)
      .\end {align*}
      </code>
    To get back to the full matrix, define <code>\mathbf {D}_i = \mathbf {P}_i \cdot  d\mathbf {P}_i</code> i.e. a vector whose <code>i</code>th entry is <code>\mathbf {P}_i \cdot  d\mathbf {P}_i</code>. Then we have that
    <code>
      \begin {align*}
        d\mathbf {S} &amp;= \mathbf {P} \odot  (d\mathbf {P} - \mathbf {D})
      .\end {align*}
      </code></li>
  <li><code>d\mathbf {Q}</code>: To get <code>d\mathbf {Q}</code>, we just apply the chain rule to <code>d\mathbf {S}</code>. Since <code>\mathbf {S} = \mathbf {Q} \mathbf {K}^T</code>,
  <code>
    \begin {align*}
       d\mathbf {Q}_{i} &amp;= \sum _{j} d\mathbf {S}_{ij} \mathbf {K}_{j} \\
                      &amp;= \sum _{j} \mathbf {P}_{ij} \cdot  (d\mathbf {P}_{ij} - \mathbf {D}_i) \mathbf {K}_{j}
    .\end {align*}
    </code></li>
  <li><code>d\mathbf {K}</code>: Same as <code>d\mathbf {Q}</code> i.e.
  <code>
    \begin {align*}
       d\mathbf {K}_{j} &amp;= \sum _{j} d\mathbf {S}_{ij} \mathbf {Q}_{i} \\
                      &amp;= \sum _{j} \mathbf {P}_{ij} \cdot  (d\mathbf {P}_{ij} - \mathbf {D}_i) \mathbf {Q}_{i}
    .\end {align*}
    </code></li></ol>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(200, 55, 45, 0.85);
  border-radius: var(--radius);
  background-color: rgba(200, 55, 45, 0.06);
  "><span style="font-weight: 600; color: rgba(200, 55, 45, 0.85);">Problem.</span> <code>\mathbf {P}</code> and <code>d\mathbf {P}</code> are <code>N \times  N</code> matrices. Materializing this is equivalent to materialing the attention matrix in the forward pass.</div>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(0, 160, 175, 0.9);
  border-radius: var(--radius);
  background-color: rgba(0, 160, 175, 0.06);
  "><span style="font-weight: 600; color: rgba(0, 160, 175, 0.9);">💡 Insight.</span> 
  <em>recomputing</em> <code>\mathbf {P}_{ij}</code> and <code>d\mathbf {P}_{ij}</code>.
  <p>First, we can easily recompute <code>\mathbf {P}_{ij}</code> with <code>\mathbf {Q}</code> and <code>\mathbf {K}</code> (which are both <code>N \times  d</code> and must be resident in memory) via</p>
  <code>\mathbf {P}_{ij} = \frac {\exp  \left (\mathbf {Q}_{i} \cdot  \mathbf {K}_{j}\right )}{\mathbf {L}_{i}}</code>
  <p>We can apply the same trick to <code>d\mathbf {P}_{ij}</code> with <code>d\mathbf {O}</code> and <code>\mathbf {V}</code> (both <code>N \times   d</code>). Namely,</p>
  <code>d\mathbf {P}_{ij} = \sum _{k} d\mathbf {O}_{ik} \mathbf {V}_{jk} = d\mathbf {O}_{i}^T \mathbf {V}_j</code>
  <p><strong>Never need an <code>N \times  N</code> matrix</strong>.</p>
</div>
<p>Now we can rewrite <code>d\mathbf {Q}</code> and <code>d\mathbf {K}</code> as
<code>
  d\mathbf {Q}_{i} = \sum _{j} \frac {\exp  \left (\mathbf {Q}_{i} \cdot  \mathbf {K}_{j}\right )}{\mathbf {L}_{i}} \cdot  (d\mathbf {O}_{i}^T \mathbf {V}_j - \mathbf {D}_i) \mathbf {K}_{j}
</code>
and
<code>
  d\mathbf {K}_{j} = \sum _{j} \frac {\exp  \left (\mathbf {Q}_{i} \cdot  \mathbf {K}_{j}\right )}{\mathbf {L}_{i}} \cdot  (d\mathbf {O}_{i}^T \mathbf {V}_j - \mathbf {D}_i) \mathbf {Q}_{i}
</code></p>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(200, 55, 45, 0.85);
  border-radius: var(--radius);
  background-color: rgba(200, 55, 45, 0.06);
  "><span style="font-weight: 600; color: rgba(200, 55, 45, 0.85);">Problem.</span> <code>\mathbf {D}</code> requires computing the <strong>global</strong> dot product <code>\mathbf {P}_i \cdot  d\mathbf {P}_i</code>.</div>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(0, 160, 175, 0.9);
  border-radius: var(--radius);
  background-color: rgba(0, 160, 175, 0.06);
  "><span style="font-weight: 600; color: rgba(0, 160, 175, 0.9);">💡 Insight.</span> 
  <p>To get around this, we precompute <code>\mathbf {D}</code> using the <code>\mathbf {O}</code> values from the forward pass and the current <code>d\mathbf {O}</code> values. In particular,</p>
  <code>
    \begin {align*}
    \mathbf {D}_i &amp;= \mathbf {P}_{i} \cdot  d\mathbf {P}_{i}  \\
    &amp;= \sum _{j} \frac {\exp  \left (\mathbf {Q}_{i} \cdot  \mathbf {K}_{j}\right )}{\mathbf {L}_{i}} d\mathbf {P}_{ij} \\
    &amp;= \sum _{j} \frac {\exp  \left (\mathbf {Q}_{i} \cdot  \mathbf {K}_{j}\right )}{\mathbf {L}_{i}} d\mathbf {O}_{i}^{T} \mathbf {V}_{j} \\
    &amp;= \sum _{j} d\mathbf {O}_{i}^{T} \left (\frac {\exp  \left (\mathbf {Q}_{i} \cdot  \mathbf {K}_{j}\right )}{\mathbf {L}_{i}} \cdot  \mathbf {V}_{j}\right ) \\
    &amp;=  d\mathbf {O}_{i}^{T} \sum _{j} \frac {\exp  \left (\mathbf {Q}_{i} \cdot  \mathbf {K}_{j}\right )}{\mathbf {L}_{i}} \cdot  \mathbf {V}_{j} \\
    &amp;= d\mathbf {O}_{i}^{T} \mathbf {O}_{i}
    .\end {align*}
    </code>
    Thus, we can compute <code>\mathbf {D}_i</code> directly from the vectors <code>d\mathbf {O}_i</code>, <code>\mathbf {O}_i</code>.
</div>

  <hr />

  <section><header><h3>Summary</h3></header>
<ul><li>In the forward pass,</li>
  <ol><li>Compute and store <code>\mathbf {O}</code> values in memory.</li> 
    <li>Compute and store <code>\mathbf {L}</code> values in memory</li></ol>
  <li>Between the forward and backward pass,</li>
  <ol><li>Compute <code>d\mathbf {O}</code>. (This is done automatically in <a href="https://kellenkanarios.com/OGCRY/">JAX</a>)</li>
    <li>Use <code>\mathbf {O}</code> and <code>d\mathbf {O}</code> to compute <code>\mathbf {D}</code>. <code>O(n)</code> memory.</li></ol>
  <li>In the backward pass,</li>
  <ol><li>Compute <code>d\mathbf {V}_j</code> via <code>\mathbf {Q}, \mathbf {K}, d\mathbf {O}</code>. <code>O(d)</code> memory.</li>
    <li>Compute <code>d\mathbf {Q}_i</code> via <code>\mathbf {Q}_i, \mathbf {K}_j, \mathbf {L}_i, \mathbf {D}_i</code>. <code>O(d)</code> memory.</li>
    <li>Compute <code>d\mathbf {K}_j</code> via <code>\mathbf {Q}_i, \mathbf {K}_j, \mathbf {L}_i, \mathbf {D}_i</code>. <code>O(d)</code> memory.</li></ol></ul>
</section>
</section>
  <div class="comments">
    
  <hr />

    <script src="https://utteranc.es/client.js" repo="kkanarios32/website-comments" issue-term="pathname" theme="boxy-light" crossorigin="anonymous" async="" />
  </div>
      </div>
    </content>
  </entry>
  <entry>
    <title>Implementing Directory-based MESI in Murphi</title>
    <published>2026-03-24T17:35:14Z</published>
    <updated>2026-03-24T17:35:14Z</updated>
    <author>
      <name>Kellen Kanarios</name>
      <uri>https://kellenkanarios.com/kellenkanarios/</uri>
    </author>
    <link rel="alternate" type="text/html" href="https://kellenkanarios.com/HHZO/" />
    <id>https://kellenkanarios.com/HHZO/</id>
    <content type="xhtml">
      <div xmlns="http://www.w3.org/1999/xhtml">
<p>This will also serve as a bit of notes from <a href="https://pages.cs.wisc.edu/~markhill/papers/primer2020_2nd_edition.pdf">A Primer on Memory Consistency and Cache Coherence</a></p><section><header><h2>Murphi</h2></header><p>Syntactical things I need to remember from Murphi docs. Most things are straightforward, but there are a few things to note:</p><section><header><h3>Declarations</h3></header><p>A <em>declaration</em> is a variable or a collection of variables that are used in the rules.</p></section><section><header><h3>Rules</h3></header><p>A <em>rule</em> is a transition from one state to another</p><ul><li>Each rule can optionally have an initial condition required to be fired</li>
    <li>We can easily define rules for all the processors or collection of <code>n</code> things with <code>ruleset</code>.</li>
    <li>For our use case, rules are used to define</li>
    <ol><li>Controller actions</li>
      <li>Receiving a request from the network</li>
      <li>Retrying stalled requests</li></ol></ul></section><section><header><h3>Startstates</h3></header><p>A <em>startstate</em> is the intial configuration for all our declarations.</p><ul><li>Must assign a value to every global variable</li></ul></section><section><header><h3>Invariants</h3></header><p>An <em>invariant</em> is something that Murphi checks to ensure is true at all possible configurations of the system.</p></section><p><strong style="font-family: var(--sans);">Overview straight from the docs.</strong> Repeat forever:
</p><ol><li>Find all rules whose conditions are true in the current state.
       (i.e. conditional expressions are true, given the current values
       of the global variables).</li>
    <li>Choose one arbitrarily and execute the action, yielding a new state.</li></ol></section>
  <section><header><h2>Starting with MSI</h2></header>
<p>The first thing we need to do is understand all the things that can go awry.</p>
<p>First in the valid-invalid protocol example we were given they only have two <a href="https://kellenkanarios.com/DWV1/">virtual channels</a>. However, we need 3 channels to handle, request messages, <strong>forwarded</strong> messages, and response messages.</p>
<p>Let us briefly understand why this is the case. Detecting a deadlock is equivalent to detecting a cycle in a dependency graph, so it is sufficient to show a cyclic dependency.</p>
<ul><li>If we issue a <code>GetS</code> this goes to the directory, giving us an edge N1 -&gt; D.</li>
  <li>Then the directory issues a <code>Fwd-GetS</code> to the ownder, giving us the chain N1 -&gt; D -&gt; N2</li>
  <li>The owner then needs to send data to the requester, giving us the chain N1 -&gt; D -&gt; N2 -&gt; N1.</li>
  <li>Namely, the data must go through the same channel as all of the requests depending on it.</li></ul>
<p>To avoid this, we partition into 3 channels:</p>
<ul><li>Request: GetS, GetM, PutM</li>
  <li>Forwarded: Fwd-GetS, Fwd-GetM, PutAck, Inv</li>
  <li>Response: InvAck, Data</li></ul>
<p>We directly enumerate these in our <code>MessageType</code> as </p>
<pre><![CDATA[  MessageType: enum {
    GetS,           -- request for shared copy
    GetM,           -- request for modified (exclusive) copy
    PutS,           -- evict shared copy
    PutM,           -- evict modified copy (w/ data)

    FwdGetS,        -- forwarded GetS to owner
    FwdGetM,        -- forwarded GetM to owner
    Inv,            -- invalidation request

    Data,           -- data response
    InvAck,         -- invalidation acknowledgment
    PutAck          -- put acknowledgment
  };]]></pre>
<strong style="font-family: var(--sans);">Cache controller.</strong> We will start with MSI cache controller, given by the following table:
<figure><img width="100%" src="https://kellenkanarios.com/bafkrmifhoq7kszlwemrlvvy4yuuvuk6l6bjadkvsmzclbemz2tzxdt6bjm.png" /></figure>
<p>Notably, we are missing some of the messages in the figures below. We can handle these based on the <code>AckCount</code></p>
<pre><![CDATA[  ProcState:
    Record
      state: enum { 
        P_Invalid, P_Shared, P_Modified,       --stable states
        PT_IS_D,      --sent GetS, waiting for Data
        PT_IS_DI,     --sent GetS, got Inv, waiting for Data then go to I
        PT_IM_AD,     --sent GetM (from I), waiting for Data and InvAcks
        PT_SM_AD,     --sent GetM (from S), waiting for Data and InvAcks
        PT_IM_A,      --got Data for GetM, waiting for remaining InvAcks
        PT_MI_A,      --sent PutM, waiting for PutAck
        PT_SI_A,      --sent PutS, waiting for PutAck
        PT_II_A       --waiting for PutAck, already invalidated
      };
      val: Value;
      ack: AckCount;  -- tracks InvAck count
    End;]]></pre>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(90, 90, 110, 0.7);
  border-radius: var(--radius);
  background-color: rgba(90, 90, 110, 0.05);
  "><span style="font-weight: 600; color: rgba(90, 90, 110, 0.7);">Remark.</span> 
The <code>load</code>, <code>store</code>, and <code>replacement</code> all only alter the states of the coherence protocol for the permanent states. Therefore, to simulate the system it is sufficient to only implement the rules that initiate these state transitions i.e. evict in modified state or load from invalid state.
</div>

<strong style="font-family: var(--sans);">Directory controller.</strong> Now for the directory controller:
<figure><img width="100%" src="https://kellenkanarios.com/bafkrmiadmfswkegttky27zg2gxvkwag3pt72ur7qam2z6vufr6xlvujqca.png" />
<figcaption /></figure>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(90, 90, 110, 0.7);
  border-radius: var(--radius);
  background-color: rgba(90, 90, 110, 0.05);
  "><span style="font-weight: 600; color: rgba(90, 90, 110, 0.7);">Remark.</span> 
We actually end up adding a second transient state to the directory controller <code>M^D</code>. This is to handle the <code>PutM</code>, <code>GetM</code> race described below.
</div>

<strong style="font-family: var(--sans);">Weird Races.</strong>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(200, 55, 45, 0.85);
  border-radius: var(--radius);
  background-color: rgba(200, 55, 45, 0.06);
  "><span style="font-weight: 600; color: rgba(200, 55, 45, 0.85);">Problem.</span> 
When implementing the <code>PutM</code> rule to downgrade from modified to invalid. We must <strong>NOT</strong> get rid of the data until we have been acknowledged, so that we can still service forward requests. This is a transition to <code>MI^A</code>. However, we can still receive the <code>PutAck</code> causing us to transition to <code>I</code> before servicing the <code>Fwd-GetM</code> request. This is lost data.
</div>


 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(40, 150, 80, 0.9);
  border-radius: var(--radius);
  background-color: rgba(40, 150, 80, 0.06);
  "><span style="font-weight: 600; color: rgba(40, 150, 80, 0.9);">Solution.</span> 
  We must add a <code>M^D</code> state to the directory controller. In this state, if we get a <code>PutM</code> request from a node that is not our current owner we stall and wait for data to arrive. For <code>M^D</code> to transition to <code>M</code>, we must send the data back to the directory even though we are doing a three-hop, so that the directory knows the data has also been forwarded to the new owner. It is unclear to me the best way to do this without this state.
</div>


 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(200, 55, 45, 0.85);
  border-radius: var(--radius);
  background-color: rgba(200, 55, 45, 0.06);
  "><span style="font-weight: 600; color: rgba(200, 55, 45, 0.85);">Problem.</span> 
 <p>The above solution causes a deadlock, where two processors both store miss and one load misses. The "winning processor" of the store will be the second one. The first one will get the data from the directory transitioning from <code>IM^{AD} \to  IM^{A}</code>, but the second one will then trigger a <code>Fwd-GetM</code> overwriting the first.</p>

<p>
The load miss transitions to <code>IS^D</code>. However, when it receives an <code>Inv</code> it stalls according to the above figure. This blocks the <code>Fwd-GetM</code> from reaching the other winning processor because they are on the same VC. Thus, we have a deadlock where the owner never gets the data.
</p>
</div>


 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(40, 150, 80, 0.9);
  border-radius: var(--radius);
  background-color: rgba(40, 150, 80, 0.06);
  "><span style="font-weight: 600; color: rgba(40, 150, 80, 0.9);">Solution.</span> We need to add another transient state not included in the figure. Namely, we add <code>II^{A}</code> that says we were in <code>IS^{A}</code>, but we got invalidated and now we are waiting for our <code>PutAck</code> before we transition to <code>I</code>.</div>


 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(90, 90, 110, 0.7);
  border-radius: var(--radius);
  background-color: rgba(90, 90, 110, 0.05);
  "><span style="font-weight: 600; color: rgba(90, 90, 110, 0.7);">Remark.</span> 
When the home directory is in <code>M</code> and we get a <code>GetS</code> we cannot clear owner as directed by figure. Otherwise in <code>S^D</code>, we will not be able to tell if the <code>PutM</code> is from the owner and stall, so we only remove the owner once we have received the data.
</div>

</section>

  <section><header><h2>Moving to MESI</h2></header>
Aside from the modifications discussed above, the rest was straightforward and we now are ready to move on to MESI.
<p><strong style="font-family: var(--sans);">Cache controller.</strong> We will start with MESI cache controller, given by the following table:
</p>
<figure><img width="100%" src="https://kellenkanarios.com/bafkrmia4fllqvr7nzhifqxwk5stk2kyje2tfokhddzfjkteyw4kbj6k25i.png" /></figure>
<p><strong style="font-family: var(--sans);">Design choice 1.</strong> Since we allow immediate upgrade to <code>M</code>, without forwarding, if E upgrades to M, then the value in the directory sent to the next <code>GetS / GetM</code> would not have updated value. It seems we must either
<ol><li>not allow E to silently upgrade to M and then we do not need forwarding, or</li>
  <li>allow E to silently upgrade to M and treat E as M basically and forward everything.</li></ol>
Notably, neither of these have to write back on <code>PutE</code>, but we will go with 1 because that is how it is indicated in the figure.
</p>
<p><strong style="font-family: var(--sans);">Design Choice 2.</strong> In order to indicate that the directory received the first <code>GetS</code> we introduce a <code>ExclusiveData</code> message. Upon receiving, <code>ExclusiveData</code> in <code>IS^D</code> we transition to <code>E</code>.</p>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(90, 90, 110, 0.7);
  border-radius: var(--radius);
  background-color: rgba(90, 90, 110, 0.05);
  "><span style="font-weight: 600; color: rgba(90, 90, 110, 0.7);">Remark.</span> 
Once we have sent the <code>PutE</code>, or the <code>PutM</code> <code>EI^A</code> and <code>MI^A</code> are functionally the same. I include both for completeness but verified that transitioning directly to <code>MI^A</code> from <code>E</code> works the same.
</div>

<p><strong style="font-family: var(--sans);">Directory controller.</strong> We start with the analagous table.</p>
<figure><img width="100%" src="https://kellenkanarios.com/bafkrmidbkxksvipiebdmkvvv27z5zth3oir5tf23olkw3w6w4hhwqttrem.png" /></figure>
<p>Similar to the cache controller, we also add a <code>E^D</code> state for completion that can be absorbed into <code>M^D</code> introduced in the MSI protocol.</p>
<p><strong style="font-family: var(--sans);">Weird Races.</strong></p>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(200, 55, 45, 0.85);
  border-radius: var(--radius);
  background-color: rgba(200, 55, 45, 0.06);
  "><span style="font-weight: 600; color: rgba(200, 55, 45, 0.85);">Problem.</span> First processor does <code>GetS</code> and is in <code>IS^D</code> but the Home directory sends it exclusive data and is now in <code>E</code>. Then second processor does <code>GetM</code> and Home issues <code>Fwd-GetM</code> to processor one, which arrives before data.</div>


 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(40, 150, 80, 0.9);
  border-radius: var(--radius);
  background-color: rgba(40, 150, 80, 0.06);
  "><span style="font-weight: 600; color: rgba(40, 150, 80, 0.9);">Solution.</span> Need to stall on <code>Fwd-GetM</code> and <code>Fwd-GetS</code> in <code>IS^D</code> until data arrives</div>


 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(200, 55, 45, 0.85);
  border-radius: var(--radius);
  background-color: rgba(200, 55, 45, 0.06);
  "><span style="font-weight: 600; color: rgba(200, 55, 45, 0.85);">Problem.</span> An even crazier race happens when we get invalidated before the <code>Fwd-GetM</code> or <code>Fwd-GetS</code> arrive. Due to our previous design decision, we transition to <code>II^A</code>, where if we receive a <code>PutAck</code> we transition to invalid. This then loses the needed forward data.</div>


 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(60, 90, 180, 0.8);
  border-radius: var(--radius);
  background-color: rgba(60, 90, 180, 0.05);
  "><span style="font-weight: 600; color: rgba(60, 90, 180, 0.8);">Example.</span> 
  The timeline:
  <ol><li><code>Proc_1</code> performs a load miss → <code>IS^D</code></li>
    <li><code>Home</code> receives first <code>GetS</code> from <code>Proc_1</code> → <code>E</code> and sends <code>ExclusiveData</code> to <code>Proc_1</code></li>
    <li><code>Proc_2</code> issues <code>GetM</code> → <code>IM^AD</code></li>
    <li><code>Home</code> sends <code>Inv</code> to <code>Proc_1</code></li>
    <li><code>Home</code> forwards <code>Fwd-GetM</code> to <code>Proc_1</code></li>
    <li><code>Proc_1</code> receives <code>Inv</code> → <code>II^D</code></li>
    <li><code>Proc_1</code> receives <code>ExclusiveData</code> → <code>I</code></li></ol>
  Data is lost
</div>


 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(40, 150, 80, 0.9);
  border-radius: var(--radius);
  background-color: rgba(40, 150, 80, 0.06);
  "><span style="font-weight: 600; color: rgba(40, 150, 80, 0.9);">Solution.</span> 
  We once again have to use the <code>ExclusiveData</code> message. We can then see that we got <code>ExclusiveData</code> in <code>II^D</code> and transition to <code>E</code> instead of <code>I</code>. <code>E</code> will then correctly handle the <code>Fwd-GetM</code> or <code>Fwd-GetS</code>.
</div>


<p>We are left with the following state diagrams for the cache and directory controllers respectively</p>
<figure><img width="100%" src="https://kellenkanarios.com/bafkrmif36mc6riik263rmccsazkkvtb5la47ocbsbm664xrm323b3jyufm.png" />
<img width="100%" src="https://kellenkanarios.com/bafkrmifqg6owj5m5bnsuqphk73uuwqzybmwczbbwx432u5j3zju3fe52i4.png" /></figure>
</section>
      </div>
    </content>
  </entry>
  <entry>
    <title>Reinforcement Learning \iff  Linear Programming</title>
    <published>2025-08-31T22:07:55Z</published>
    <updated>2025-08-31T22:07:55Z</updated>
    <author>
      <name>Kellen Kanarios</name>
      <uri>https://kellenkanarios.com/kellenkanarios/</uri>
    </author>
    <link rel="alternate" type="text/html" href="https://kellenkanarios.com/J3CP/" />
    <id>https://kellenkanarios.com/J3CP/</id>
    <content type="xhtml">
      <div xmlns="http://www.w3.org/1999/xhtml">

  <section><header><h2>Linear Program</h2></header>
    For this note, we will consider a <em>maximization</em> linear program. Namely, an optimization problem of the following form
    <code>
        \begin {align*}
        \max _{\mathbf {x}} \quad  &amp;\mathbf {c}^{\top } \mathbf {x} \\
        \text {s.t.} \quad  &amp;A\mathbf {x} \leq  b
        .\end {align*}
        </code>
        In the spirit of <em>lagrange multipliers</em>, we can also get a <em>dual formulation</em>. To do so, we add a multiplier <code>y_i</code> for each constraint i.e. 
        <code>y_i \mathbf {a}_i^T \mathbf {x} \leq  y_i b_i</code>
        we then consider the summed constraint (which ends up being tight enough),
        <code>\mathbf {y}^T A \mathbf {x} \leq  \mathbf {y}^T\mathbf {b}</code>
        To relate this back to our primal problem, we enforce the additional constraint that <code>\mathbf {y}^T A = \mathbf {c}^T \iff  A \mathbf {y} = \mathbf {c} </code>. The reason for this is that for a feasible <code>\mathbf {y}</code>, we have
        <code>\mathbf {y}^T A \mathbf {x} = \mathbf {c}^T \mathbf {x} \leq  \mathbf {y}^T \mathbf {b},  </code>
        so that <code>\mathbf {y}^T \mathbf {b}</code> is an upper bound for our primal objective <code>\mathbf {c}^T \mathbf {x} </code>. Intuitively, to recover <code>\mathbf {c}^T \mathbf {x}^* </code>, we can just minimize over <code>\mathbf {y}</code>, resulting in the following <em>dual</em> program
        <code>
            \begin {align*}
             \min _{\mathbf {y}} \quad  &amp;\mathbf {b}^T\mathbf {y} \\   
             \text {s.t.} \quad  &amp;A \mathbf {y} = \mathbf {c} \\ 
             &amp; \mathbf {y} \geq  0
            .\end {align*}
            </code>
</section>

  <section><header><h2>RL as Linear Program</h2></header>
    Let <code>\rho (s) \in  \mathbb {R}^{|\mathcal {S}|} </code> be the initial state distribution. Then consider the following LP 
    <code>\begin {align*}
\begin {array}{ll} \displaystyle  \max _{V \in  \mathbb {R}^{|\mathcal {S}|}} &amp; \displaystyle  \sum _{s \in  \mathcal {S}} \rho (s) V(s) \\ \text {s.t.} &amp; V(s) \leq  r(s,a) + \gamma  \sum _{s'} P(s' \mid  s,a) \, V(s'), \qquad  \forall  s,a. \end {array}
    \end {align*}</code>
    Translating this into our standard form, we have
    <ol><li><code>\mathbf {c} \in  \mathbb {R}^{|\mathcal {S}|}</code> with <code>c_s = \rho (s)</code></li>
      <li><code>A \in  \mathbb {R}^{|\mathcal {S}| \times  |\mathcal {A}| \times  |\mathcal {S}|}</code> with <code>A_{(s,a),s'} = -\delta _{s,s'} + \gamma  P(s'|s,a).
</code></li></ol>
    Substituting this in for the dual formulation gives us
    <code>\begin {align*}
&amp; \min _{y(s,a) \geq  0} &amp; &amp; \sum _{s,a} r(s,a) \, y(s,a) \\ &amp; \text {s.t.} &amp; &amp; \sum _{a} y(s,a) - \gamma  \sum _{s',a'} P(s \mid  s',a') \, y(s',a') = \rho (s), \qquad  \forall  s
    \end {align*}</code>
    Here <code>\mathbf {y}</code> can be seen as the <em>discounted occupancy measure</em>. This allows us to recover a randomized optimal policy via
    <code>\mathbb {P}(\pi ^*(s) = a) = \frac {y(s, a)}{\sum _{a} y(s,a)}</code>
</section>

  <div class="comments">
    
  <hr />

    <script src="https://utteranc.es/client.js" repo="kkanarios32/website-comments" issue-term="pathname" theme="boxy-light" crossorigin="anonymous" async="" />
  </div>
      </div>
    </content>
  </entry>
  <entry>
    <title>Rapidly Exploring Random Trees</title>
    <published>2025-08-14T23:31:25Z</published>
    <updated>2025-08-14T23:31:25Z</updated>
    <author>
      <name>Kellen Kanarios</name>
      <uri>https://kellenkanarios.com/kellenkanarios/</uri>
    </author>
    <link rel="alternate" type="text/html" href="https://kellenkanarios.com/QQC1/" />
    <id>https://kellenkanarios.com/QQC1/</id>
    <content type="xhtml">
      <div xmlns="http://www.w3.org/1999/xhtml">
<p>Rapidly exploring random trees (RRT) is a very simple, elegant and widely used algorithm for motion planning. The algorithm proceeds as follows:</p>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(0, 100, 255, 0.7);
  border-radius: var(--radius);
  background-color: rgba(0, 100, 255, 0.05);
  "><span style="font-weight: 600; color: rgba(0, 100, 255, 0.7);">Algorithm.</span> 
  For <code>k = 1</code> to <code>K</code> do
 <ol><li><code>x_{rand} \gets  \mathsf {RANDOM\_STATE}()</code></li>
  <li><code>x_{near} \gets  \mathsf {NEAREST\_NEIGHBOR}(x_{rand}, \mathcal {T})</code></li>
  <li><code>u \gets  \mathsf {SELECT\_INPUT}(x_{near})</code></li>
  <li><code>x_{new} \gets  \mathsf {NEW\_STATE}(x_{near}, u, \Delta  t)</code></li>
  <li><code>\mathcal {T}.add\_vertex(x_{new})</code></li>
  <li><code>\mathcal {T}.add\_edge((x_{near}, u, x_{new}))</code></li></ol>
 return <code>\mathcal {T}</code>
</div>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(90, 90, 110, 0.7);
  border-radius: var(--radius);
  background-color: rgba(90, 90, 110, 0.05);
  "><span style="font-weight: 600; color: rgba(90, 90, 110, 0.7);">Remark.</span> 
Given some dynamics model <code>\dot {x} = f(x, u)</code>, <code>\mathsf {NEW\_STATE}</code> is some approximate integration, such as Euler integration i.e.
<code>x_{new} = x_{near} + f(x_{near}, u) \Delta  t</code>
Preferable to use higher-order method like Runge-Kutta.
</div>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(90, 90, 110, 0.7);
  border-radius: var(--radius);
  background-color: rgba(90, 90, 110, 0.05);
  "><span style="font-weight: 600; color: rgba(90, 90, 110, 0.7);">Remark.</span> 
<code>\mathsf {SELECT\_INPUT}</code> is determined by whether the system is holonomic or non-holomonic. If the agent is holonomic, then <code>u</code> will just be the control corresponding to the difference in the two states.
</div>

  <div class="comments">
    
  <hr />

    <script src="https://utteranc.es/client.js" repo="kkanarios32/website-comments" issue-term="pathname" theme="boxy-light" crossorigin="anonymous" async="" />
  </div>
      </div>
    </content>
  </entry>
  <entry>
    <title>Eligibility Traces</title>
    <published>2025-07-24T21:14:08Z</published>
    <updated>2025-07-24T21:14:08Z</updated>
    <author>
      <name>Kellen Kanarios</name>
      <uri>https://kellenkanarios.com/kellenkanarios/</uri>
    </author>
    <link rel="alternate" type="text/html" href="https://kellenkanarios.com/DJPO/" />
    <id>https://kellenkanarios.com/DJPO/</id>
    <content type="xhtml">
      <div xmlns="http://www.w3.org/1999/xhtml">
<p>As I grow more interested in abandoning batched updates and the replay buffer, the importance of elibility traces grows with me. Due to this, I thought it finally time to re-prove / understand what makes these traces so eligible.</p><p>The reference followed is <a href="https://kellenkanarios.com/suttonReinforcementLearningIntroduction2018/">Sutton and Barto</a> (Ch 12.).</p><p>The eligibility trace vector is given by
<code>
\begin {align*}
 \mathbf {z}_{-1} &amp;= 0 \\
 \mathbf {z}_t &amp;= \gamma  \lambda  \mathbf {z}_{t - 1} + \nabla  \hat {v}(S_t; \mathbf {w}_t)
.\end {align*}
</code>
Recall the temporal difference error is given by
<code>\begin {align*}
  \mathbf {\delta }_t = r_{t + 1} + \gamma  \hat {v}(s_{t + 1}; \mathbf {w}_t) - \hat {v}(s_t; \mathbf {w}_t)
.\end {align*}</code>
For TD(<code>\lambda </code>), the incremental update is given by
<code>\begin {align*}
  \mathbf {w}_{t + 1} = \mathbf {w}_t + \alpha  \mathbf {\delta }_{t} \mathbf {z}_t
.\end {align*}</code></p><p>We can rewrite the <code>\lambda </code>-return recursively in terms of reward and itself as follows:
<code>\begin {align*}
  G_t^{\lambda } &amp;= (1 - \lambda ) \sum _{n=1}^{\infty } \lambda ^{n-1}G_{t:t+n} \\
  G_t^{\lambda } &amp;= (1 - \lambda ) \sum _{n=1}^{\infty } \lambda ^{n-1}(R_{t + 1} + \gamma  G_{t+1:t+n}) \\
  G_t^{\lambda } &amp;= R_{t + 1} + \gamma  (1 - \lambda ) \left [\hat {v}(S_{t + 1}; \mathbf {w}_t) + \sum _{n=2}^{\infty } \lambda ^{n-1} G_{t+1:t+n}\right ] \\
  G_t^{\lambda } &amp;= R_{t + 1} + (1 - \lambda )\gamma  \hat {v}(S_{t + 1}; \mathbf {w}_t) + \gamma  \lambda  G_{t + 1}^{\lambda }
.\end {align*}</code></p><p>Now we will show that the prediction error of TD(<code>\lambda </code>) can be re-written as the sum of TD-errors. The prediction error is written as
<code>PE = G_t^{\lambda } - \hat {v}(S_t, \mathbf {w}_t)</code>
Rewriting the <code>\lambda </code>-return as above
<code>
\begin {align*}
 PE &amp;= R_{t + 1} + \gamma  \hat {v}(S_{t + 1} ; \mathbf {w}_t) + \gamma  \lambda  G_{t + 1}^{\lambda } - \gamma  \lambda  \hat {v}(S_{t + 1}, \mathbf {w}_t) - \hat {v}(S_t; \mathbf {w}_t) 
 &amp;= (R_{t + 1} + \gamma  \hat {v}(S_{t + 1} ; \mathbf {w}_t) - \hat {v}(S_t, \mathbf {w}_t)) + \gamma  \lambda  G_{t + 1}^{\lambda } - \gamma  \lambda  \hat {v}(S_{t + 1}; \mathbf {w}_t)
 &amp;= \mathbf {\delta }_t  + \gamma  \lambda  G_{t + 1}^{\lambda } - \gamma  \lambda  \hat {v}(S_{t + 1}; \mathbf {w}_t)
.\end {align*}
</code>
<p>
We now are left with <code>\gamma  \lambda  (G_{t + 1}^{\lambda } - \hat {v}(S_{t + 1}, \mathbf {w}_t))</code>, where we can proceed in the same manner recursively, such that
<code>PE = \sum _{t=1}^{\infty } (\gamma  \lambda )^{t - 1} \delta _t</code>
It is easy to see the same thing goes through for the finite time slice <code>\lambda </code>-return i.e.
<code>G_{t:t + k}^{\lambda } - \hat {v}(S_t, \mathbf {w}_{t - 1}) = \sum _{i = t}^{t + k} (\gamma  \lambda )^{i - t} \delta _i</code></p>
<p>
  Going back to the eligibility trace update, if we instead do not apply each elibgibility trace update but apply the sum of all the updates for the same fixed <code>\mathbf {w}_t</code>, then we get that the total update is
<code>
  \begin {align*}
   \mathbf {w}_{t+1} &amp;= \sum _{t=1}^{T} \alpha  \delta _t \mathbf {z}_t \\
   &amp;= \sum _{t=1}^{T} \alpha  \delta _t \sum _{i=0}^{t} (\lambda  \gamma )^{i} \nabla  \hat {v}(S_i, \mathbf {w}_t) \\
   &amp;= \sum _{t=1}^{T} \alpha  (r_t + \gamma  \hat {v}(S_{t+1}, \mathbf {w}_t) - \hat {v}(S_t, \mathbf {w}_t)) \underbrace {\sum _{i=0}^{t} (\lambda  \gamma )^{i} \nabla  \hat {v}(S_i, \mathbf {w}_t)}_{X_t}
  .\end {align*}
  </code>
  Then
  <code>\begin {align*}
   &amp;= \sum _{t=1}^{T} \alpha  r_t X_t + \gamma  \hat {v}(S_{t+1}, \mathbf {w}_t) X_t - \hat {v}(S_t, \mathbf {w}_t) X_t
  .\end {align*}</code></p>
<p>
  This shows that the <em>backward-view</em> and <em>forward-view</em> are "equivalent". The forward-view in practice makes an update at each step, so it is typically better.
</p></p>
  <div class="comments">
    
  <hr />

    <script src="https://utteranc.es/client.js" repo="kkanarios32/website-comments" issue-term="pathname" theme="boxy-light" crossorigin="anonymous" async="" />
  </div>
      </div>
    </content>
  </entry>
  <entry>
    <title>Multi Query Attention</title>
    <published>2025-07-14T17:21:58Z</published>
    <updated>2025-07-14T17:21:58Z</updated>
    <author>
      <name>Kellen Kanarios</name>
      <uri>https://kellenkanarios.com/kellenkanarios/</uri>
    </author>
    <link rel="alternate" type="text/html" href="https://kellenkanarios.com/W4YP/" />
    <id>https://kellenkanarios.com/W4YP/</id>
    <content type="xhtml">
      <div xmlns="http://www.w3.org/1999/xhtml">
<p>Notes on the paper <a href="https://kellenkanarios.com/shazeerFastTransformerDecoding2019/">Fast Transformer Decoding: One Write-Head is All You Need</a>.</p><p>For, 
<ul><li><code>b</code> batch dimension,</li>
  <li><code>n</code> sequence length,</li>
  <li><code>d</code> hidden dimension,</li>
  <li><code>h</code> number of heads.</li></ul>
The total <a href="https://kellenkanarios.com/4K5H/">FLOPS</a> for <a href="https://kellenkanarios.com/004G/">attention</a> are roughly <code>bnd^2</code>. This comes from 
<ol><li><code>3nd^2</code> FLOPS to compute <code>Q = XW_Q, K = XW_K, V = XW_V</code>.</li>
  <li>Need to do this <code>b</code> times for each input in batch.</li></ol>
and the total memory accesses are roughly <code>\underbrace {bnd}_{X} + \underbrace {bhn^2}_{\text {softmax}} + \underbrace {d^2}_{\text {projection}}</code>.
This gives high arithmetic intensity
<code>O\left (\left (\frac {h}{d} + \frac {1}{bn}\right )^{-1}\right )</code></p><p>However, if we do incremental inference then we must multiply our total number of memory accesses by <code>n</code> i.e. <code>bn^2d + nd^2</code>. This gives arithmetic intensity
<code>O\left (\left (\frac {n}{d} + \frac {1}{b}\right )^{-1}\right ),</code>
which requires large batch and short sequence length.
</p>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(90, 90, 110, 0.7);
  border-radius: var(--radius);
  background-color: rgba(90, 90, 110, 0.05);
  "><span style="font-weight: 600; color: rgba(90, 90, 110, 0.7);">Remark.</span> 
I believe we just ignore the softmax memory contribution in the inference case because it does not scale with <code>n</code> anymore as we are computing the logits just for the next token. Therefore, it becomes a <code>\frac {d}{h}</code> term, which we can safely ignore?
</div>
<p>The key is that the <code>\frac {n}{d}</code> term comes from the <code>bn^2d</code> term, where we are loading <code>h</code> <code>(b \times  n \times  d / h)</code> <code>K</code> and <code>V</code> matrices <code>n</code> times. Thus, we can improve the <code>\frac {n}{d}</code> term by a factor of <code>h</code> by simply not using a different <code>K</code> and <code>V</code> matrix for each head. This is the entire idea behind <a href="https://kellenkanarios.com/shazeerFastTransformerDecoding2019/">MQA</a>.</p>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(90, 90, 110, 0.7);
  border-radius: var(--radius);
  background-color: rgba(90, 90, 110, 0.05);
  "><span style="font-weight: 600; color: rgba(90, 90, 110, 0.7);">Remark.</span> 
Similar to the <a href="https://kellenkanarios.com/TJLA/">KV-cache</a>, we still can use <code>h</code> <code>Q</code> matrices because we do not need to load <code>n</code> of them into memory because only the last one matters for inference.
</div>
<section><header><h2>Group Query Attention</h2></header><p><em>Group query attention</em> is the same idea as <a href="https://kellenkanarios.com/shazeerFastTransformerDecoding2019/">MQA</a> but instead of using one <code>K</code> and <code>V</code> for every head, they use a <em>group</em> of them. Where obviously the number of groups needs to be less than the number of heads.</p><figure><img width="80%" src="https://kellenkanarios.com/bafkrmicmjlgi2o3oetsssjdwk6y2pqce4vrdwiw2ah4wwe3ulnt74upkiu.png" />
<figcaption>Grouped query uses subset of <code>K</code> and <code>V</code> matrices.</figcaption></figure></section><p>Also inspired by this idea of reducing the dependence of <code>K</code> and <code>V</code> on the sequence length is sliding window attention.</p><section><header><h2>Sliding Window Attention</h2></header><p>The idea behind <em>sliding window attention</em> is to ensure the number of weight matrices needed for the keys <code>K</code> and values <code>V</code> does not scale with the sequence length. Intuitively, this means allowing each word to "attend" to only some fixed number of previous words rather than the whole sequence</p><figure><img width="80%" src="https://kellenkanarios.com/bafkrmidqeoo6wx6s4ry6u74fcelfzxvwreyf7d44whnql4ffnnyd2dwcha.png" />
<figcaption>"the" only sees "on" and "sat" rather than the full sentence.</figcaption></figure></section>
  <div class="comments">
    
  <hr />

    <script src="https://utteranc.es/client.js" repo="kkanarios32/website-comments" issue-term="pathname" theme="boxy-light" crossorigin="anonymous" async="" />
  </div>
      </div>
    </content>
  </entry>
  <entry>
    <title>Expectation Maximization</title>
    <published>2025-07-01T17:03:45Z</published>
    <updated>2025-07-01T17:03:45Z</updated>
    <author>
      <name>Kellen Kanarios</name>
      <uri>https://kellenkanarios.com/kellenkanarios/</uri>
    </author>
    <link rel="alternate" type="text/html" href="https://kellenkanarios.com/0088/" />
    <id>https://kellenkanarios.com/0088/</id>
    <content type="xhtml">
      <div xmlns="http://www.w3.org/1999/xhtml">
<p>This is the follow up to the previous note on the <a href="https://kellenkanarios.com/0082/">ELBO</a>. Mainly, here we aim to show how we can use the ELBO to find <code>\theta ^*</code> i.e. 
<code>
\begin {align*}
 \theta ^* &amp;= \arg \max  \mathbb {E}_{x \sim  \mathcal {D}} \log  p(x; \theta ) \\ 
 &amp;= \arg \max  \mathbb {E}_{x \sim  \mathcal {D}} \log  \int  p(x, z; \theta ) \mathrm {d}z 
.\end {align*}
</code>
Notably, <a href="https://kellenkanarios.com/0082/">ELBO</a> gives us the lower bound 
<code>\log  p(x; \theta ) \geq  \mathbb {E}_{z \sim  q}\left [\log  \frac {p(x,  Z; \theta )}{q(z)}\right ]  \eqqcolon  \mathsf {ELBO}(x, q, \theta )</code>
for a <strong>fixed</strong> theta <strong>and</strong> <code>q</code>. Therefore, <strong>for each</strong> <code>\theta  </code>, we want to find some <code>q</code>, such that
  <code>\mathsf {ELBO}(x, q, \theta ) = \log  p(x, \theta )</code>
  With this, we can easily perform gradient ascent
  <code>\theta _{t + 1} \gets  \theta _t + \nabla _{\theta } \mathsf {ELBO}(x, q, \theta _t),</code>
  to maximize the log-likelihood <code>\log  p(x; \theta )</code>. 
</p>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(90, 90, 110, 0.7);
  border-radius: var(--radius);
  background-color: rgba(90, 90, 110, 0.05);
  "><span style="font-weight: 600; color: rgba(90, 90, 110, 0.7);">Remark.</span> 
Note that the gradient is with respect to <code>\theta </code>, where <code>\theta </code> is just the third argument to <code>\mathsf {ELBO}</code> as defined above. Notably, <code>\theta _t</code> will show up elsewhere later but will <strong>NOT</strong> be backpropagated through.
</div>

  <section><header><h2>Expectation</h2></header>
<p>The above motivates the <em>expectation</em> step of the EM algorithm. Namely how can we compute the lower bound expectation for the <strong>best</strong> possible <code>q</code>.</p>
<p>Recall (or review) from the discussion on <a href="https://kellenkanarios.com/0082/">ELBO</a> that <code>q(z) = p(z | x; \theta )</code> maximizes the ELBO for a given <code>x</code> and <code>\theta  </code>. Therefore, we actually want to learn <code>q_{\phi }(z, x, \theta ) \approx  p(z | x; \theta )</code>. To do so, we can simply maximize the ELBO (duh). Formally,
<code>\phi _{t+1} = \argmax _{\phi } \mathbb {E}_{x \sim  \mathcal {D}, z \sim  q}\left [\log  \frac {p(x,  Z; \theta _t)}{q_{\phi }(Z, x, \theta _t)}\right ]</code></p>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(200, 130, 0, 0.9);
  border-radius: var(--radius);
  background-color: rgba(200, 130, 0, 0.06);
  "><span style="font-weight: 600; color: rgba(200, 130, 0, 0.9);">Question.</span> 
Now this is all cool theoretically but how TF do we actually maximize this thing? 
</div>


 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(0, 140, 120, 0.9);
  border-radius: var(--radius);
  background-color: rgba(0, 140, 120, 0.06);
  "><span style="font-weight: 600; color: rgba(0, 140, 120, 0.9);">Answer.</span> 
  It is highly dependent on the parametrization of <code>p</code> and <code>q</code>.
</div>


 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(60, 90, 180, 0.8);
  border-radius: var(--radius);
  background-color: rgba(60, 90, 180, 0.05);
  "><span style="font-weight: 600; color: rgba(60, 90, 180, 0.8);">Example.</span> 
  <p>
  We define the problem as 
  <ol><li><code>p(z) \sim  N(0, 1)</code></li>
    <li><code>p(x | z; \theta ) \sim  N(\mu _{\theta }(z), \sigma _{\theta }(z))</code></li>
    <li><code>q_{\phi }(z | x, \theta ) \sim  N(\mu _{\phi }(x), \sigma _\phi (x))</code></li></ol>
  where <code>\mu _{\{\phi , \theta \}}, \sigma _{\{\phi , \theta \}}</code> are parametrized neural networks. Then we can rewrite the <a href="https://kellenkanarios.com/0082/">ELBO</a> as (ignoring constants)
  <code>
    \begin {align*}
     \mathsf {ELBO}(x, q, \theta ) &amp;= \mathbb {E}_{x \sim  \mathcal {D}, z \sim  q(\cdot  \mid  x, \theta )} \left [\log  p(Z) - \log  \frac {p(x \mid  Z; \theta )}{q_{\phi }(Z, x, \theta )}\right ] \\
&amp;= \mathbb {E}_{x \sim  \mathcal {D}, z \sim  q(\cdot  \mid  x, \theta )} \left [-\frac {Z^2}{2}  - \frac {\sigma _{\phi }(x)^2}{\sigma _{\theta }(z)^2}\big ((Z - \mu _{\theta }(Z))^2 - (Z - \mu _{\phi }(x))^2\big )\right ]
    .\end {align*}
    </code></p>
</div>

  
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(200, 55, 45, 0.85);
  border-radius: var(--radius);
  background-color: rgba(200, 55, 45, 0.06);
  "><span style="font-weight: 600; color: rgba(200, 55, 45, 0.85);">Problem.</span> 
    We need to sample realization <code>z \sim  q_{\phi }(\cdot  |x, \theta )</code>. So isn't our realization <code>z</code> a function of <code>\phi </code>. How would we optimize this?
  </div>

  
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(0, 140, 120, 0.9);
  border-radius: var(--radius);
  background-color: rgba(0, 140, 120, 0.06);
  "><span style="font-weight: 600; color: rgba(0, 140, 120, 0.9);">Answer.</span> 
    The <a href="https://kellenkanarios.com/0089/">reparametrization trick</a>!!
  </div>

  <section><header><h3>Reparametrization Trick</h3></header><p>For <code>q_{\phi }(\cdot  | x) \sim  N(\mu _{\phi }(x), \sigma _{\phi }(x))</code>. The <em>reparametrization trick</em> allows us to differentate through a random sample by sampling from <code>q_{\phi }(\cdot  | x)</code> via 
<code>z(\phi ) = \mu _{\phi }(x) + \sigma _{\phi }(x) \cdot  \epsilon ,</code>
where <code>\epsilon  \sim  N(0, 1)</code>. Then, for a given sample <code>\epsilon </code>, we can easily compute
<code>\nabla _{\phi } z(\phi ) = \nabla _{\phi } \mu _{\phi }(x) + \nabla _{\phi }\sigma _{\phi }(x) \cdot  \epsilon </code></p></section>
  <p>Now for a given sample <code>x_i \sim  \mathcal {D}</code>, we can get a sample <code>z_i(\phi ) \sim  q(\cdot  \mid  x_i, \theta )</code> that is differentiable with respect to <code>\phi </code>. Using this, we can get the stochastic gradient estimate
  <code>\nabla _q \widetilde {\mathsf {ELBO}}(x, q, \theta ) = \nabla _{\phi } \left [\frac {z_i(\phi )^2}{2}  - \frac {\sigma _{\phi }(x)^2}{\sigma _{\theta }(z)^2}\big ((z_i(\phi ) - \mu _{\theta }(z_i(\phi )))^2 - (z_i(\phi ) - \mu _{\phi }(x))^2\big )\right ]</code></p>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(0, 100, 255, 0.7);
  border-radius: var(--radius);
  background-color: rgba(0, 100, 255, 0.05);
  "><span style="font-weight: 600; color: rgba(0, 100, 255, 0.7);">Algorithm.</span> 
  input <code>\theta _t</code>
  <ol><li>Sample <code>x_i</code> from dataset <code>\mathcal {D}</code></li>
    <li>Sample <code>z_i(\phi ) \sim  q_{\phi }(\cdot  \mid  x_i, \theta _t)</code> via <a href="https://kellenkanarios.com/0089/">trick</a>.</li>
    <li>Compute <code>\nabla _q \widetilde {\mathsf {ELBO}}</code></li>
    <li>Update via SGD</li></ol>
</div>

</section>

  <section><header><h2>Maximization</h2></header>
<p>Finally, we have reached the maximization step. This step is fairly straightforward after all the progress we have made. Once we have a tight lower bound i.e. <code>q(z) \approx  p(z \mid  x; \theta )</code> then we simply fix <code>q</code> and maximize the <a href="https://kellenkanarios.com/0082/">ELBO</a> for <code>\theta </code> i.e. in the above example and algorithm we instead compute
<code>\nabla _\theta  \widetilde {\mathsf {ELBO}}(x, q, \theta ) = \nabla _{\theta } \left [\frac {z_i(\phi )^2}{2}  - \frac {\sigma _{\phi }(x)^2}{\sigma _{\theta }(z)^2}\big ((z_i(\phi ) - \mu _{\theta }(z_i(\phi )))^2 - (z_i(\phi ) - \mu _{\phi }(x))^2\big )\right ]</code>
Alternating between these steps yields the <em>expectation-maximization</em> algorithm the ML oldheads mythologize.
</p>
</section>

  <div class="comments">
    
  <hr />

    <script src="https://utteranc.es/client.js" repo="kkanarios32/website-comments" issue-term="pathname" theme="boxy-light" crossorigin="anonymous" async="" />
  </div>
      </div>
    </content>
  </entry>
  <entry>
    <title>Evidence Lower Bound</title>
    <published>2025-06-30T20:59:54Z</published>
    <updated>2025-06-30T20:59:54Z</updated>
    <author>
      <name>Kellen Kanarios</name>
      <uri>https://kellenkanarios.com/kellenkanarios/</uri>
    </author>
    <link rel="alternate" type="text/html" href="https://kellenkanarios.com/0082/" />
    <id>https://kellenkanarios.com/0082/</id>
    <content type="xhtml">
      <div xmlns="http://www.w3.org/1999/xhtml">
<p><strong style="font-family: var(--sans);">Setup.</strong>
We are given a joint distribution <code>p(X, Z; \theta )</code>, where <code>X</code> and <code>Z</code> are random variables with their joint parametrized by <code>\theta </code>. We are also given a dataset <code>\mathcal {D}</code> containing realizations of <strong>only</strong> <code>X</code> i.e. <code>Z</code> is a <a href="https://kellenkanarios.com/0083/">latent random variable</a>.
  </p>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(190, 50, 130, 0.85);
  border-radius: var(--radius);
  background-color: rgba(190, 50, 130, 0.06);
  "><span style="font-weight: 600; color: rgba(190, 50, 130, 0.85);">Goal.</span> We want to learn <code>p(x; \theta )</code>.</div>
<p> However, <code>p(x; \theta ) = \int  p(x, z; \theta ) \mathrm {d}z = \int  p(x | z; \theta )p(z) \mathrm {d}z,</code> which is computationally intractable for most continuous <code>Z</code>.</p>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(200, 130, 0, 0.9);
  border-radius: var(--radius);
  background-color: rgba(200, 130, 0, 0.06);
  "><span style="font-weight: 600; color: rgba(200, 130, 0, 0.9);">Question.</span> Can we update <code>\theta </code> for a given realization of <code>x</code> and <code>z</code>?</div>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(0, 140, 120, 0.9);
  border-radius: var(--radius);
  background-color: rgba(0, 140, 120, 0.06);
  "><span style="font-weight: 600; color: rgba(0, 140, 120, 0.9);">Answer.</span> Yes!!!</div>
<p><strong style="font-family: var(--sans);">Motivation.</strong> Suppose we had some <code>q(z)</code> that we could sample realizations of <code>Z</code> from. Then we want to find some reasonable <code>\ell _{\theta }</code>, such that
<code>\theta ^* \approx  \arg \max _{\theta } \mathbb {E}_{x, \sim  p(x; \theta ^*), z \sim  q(z)}[\ell _{\theta }(X, Z)]</code>

 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(90, 90, 110, 0.7);
  border-radius: var(--radius);
  background-color: rgba(90, 90, 110, 0.05);
  "><span style="font-weight: 600; color: rgba(90, 90, 110, 0.7);">Remark.</span> 
<code>q(z)</code> can potentially (and will) depend on the sample <code>x_i</code>. This is OK because we are given <code>x_i</code>'s, which we can then just pass to <code>q</code> to get the corresponding <code>z_i</code>.
</div>

Why is this the case? Because then (for well-behaved <code>\ell _{\theta }</code>)
<code>\theta _{t + 1} = \theta _{t} - \alpha  \nabla _{\theta _{t}}\ell _{\theta _{t}}(x_i, z_i) \to  \theta ^*</code>
i.e. SGD is a reasonable thing to do, where the gradient is for a <strong>single</strong> sample of <code>x_i</code> and <code>z_i</code> which we have readily available from our dataset <code>\mathcal {D}</code> and our distribution <code>q</code> that we can sample from. This idea comes up a lot in optimization, such as the <code>\log </code> trick in the original policy gradient.
</p><p>Similar to the <code>\log </code> trick, the <em>Evidence Lower Bound</em> (ELBO) comes from a simple multiply and divide trick. Namely,
<code>\begin {align*}
  \log  p(x; \theta ) &amp;= \log  \int  p(x, z; \theta ) \mathrm {d}z \\
  &amp;= \log  \int  p(x, z; \theta ) \frac {q(z)}{q(z)} \mathrm {d}z \\
  &amp;= \log  \mathbb {E}_{z \sim  q(z)} \frac {p(x, Z; \theta )}{q(z)} \\
  &amp;\geq  \mathbb {E}_{z \sim  q(z)} \log  \frac {p(x, Z; \theta )}{q(z)}
.\end {align*}</code>
The last inequality follows directly from <a href="https://kellenkanarios.com/002G/">Jensen's Inequality</a>. However, note that this is only a lower bound and therefore not exactly what we want. For example, if we were to pick <code>q \sim  N(0, 1)</code> and then optimize the ELBO for <code>\theta </code> we would not necessarily find a good <code>\theta </code> for the likelihood <code>\log  p(x; \theta )</code>. Luckily, we can actually get the difference <code>\log  p(x; \theta ) - </code>ELBO in closed form! Namely,
<code>\begin {align*}
  \log  p(x; \theta ) - \mathrm {ELBO} &amp;= \log  p(x; \theta ) - \mathbb {E}_{z \sim  q(z)} \log  \frac {p(x, Z; \theta )}{q(z)} \\ 
&amp;= \int  \left (\log  p(x; \theta ) - \log  \frac {p(x, z; \theta )}{q(z)}\right )q(z) \\ 
&amp;= \int  \left (\log  \frac {p(x; \theta )q(z)}{p(x, z; \theta )}\right )q(z) \\ 
&amp;= \int  \left (\log  \frac {p(x; \theta )q(z)}{p(z | x; \theta )p(x; \theta )}\right )q(z) \\ 
&amp;= \int  \left (\log  \frac {q(z)}{p(z | x; \theta )}\right )q(z) \\ 
&amp;= \mathrm {KL}\big (q(z) \mid \mid  p(z | x; \theta )\big )
.\end {align*}</code>
Thus, to tighten the lower bound we can optimize <code>q_{\phi }(z)</code> to try to match <code>p(z | x; \theta )</code>. As a sanity check, lets make sure these match when <code>q(z) = p(z \mid  x; \theta )</code>. 
<code>\begin {align*}
  \mathbb {E}_{z \sim  q(z)} \log  \frac {p(x, Z; \theta )}{q(z)} &amp;= \int  \log  \frac {p(x, z; \theta )}{q(z)}\mathrm {d}z \\
  &amp;= \int  \log  \frac {p(x; \theta )p(z \mid  x; \theta )}{p(z \mid  x; \theta )}\mathrm {d}z \\
  &amp;= \int  \log  p(x; \theta )\mathrm {d}z \\
  &amp;= \log (x; \theta )
.\end {align*}</code>
You may be wondering how we use this algorithmically to find <code>\theta ^*</code>? This will be the main idea of <a href="https://kellenkanarios.com/0088/">expectation maximization</a>. Feel free to check it out!
</p>
  <div class="comments">
    
  <hr />

    <script src="https://utteranc.es/client.js" repo="kkanarios32/website-comments" issue-term="pathname" theme="boxy-light" crossorigin="anonymous" async="" />
  </div>
      </div>
    </content>
  </entry>
  <entry>
    <title>Rebuilding My (Neo)Vim Config From Scratch</title>
    <published>2025-03-04T00:00:00Z</published>
    <updated>2025-03-04T00:00:00Z</updated>
    <author>
      <name>Kellen Kanarios</name>
      <uri>https://kellenkanarios.com/kellenkanarios/</uri>
    </author>
    <link rel="alternate" type="text/html" href="https://kellenkanarios.com/005F/" />
    <id>https://kellenkanarios.com/005F/</id>
    <content type="xhtml">
      <div xmlns="http://www.w3.org/1999/xhtml">
<p>I used LazyVim for a long time, but I kept hitting a wall: understanding <em>how</em> LazyVim did something was consistently harder than if I had just written the thing myself. So I gave myself one day to rebuild from scratch, with the modest goal of first-class support for <code>\TeX </code>, Python, Forester, and C/C++. That one-day sprint is long past, and the config has kept evolving since — this note describes where it landed, not the frantic first pass. The through-line is that almost everything now leans on Neovim's own primitives instead of a framework, which is exactly the transparency I was missing.</p>
 <div class="jms-callout" style="
  margin: 1em 0 1em 0.25em;
  padding: 0.5em 1.1em 0.5em 1.2em;
  border-left: 4px solid rgba(90, 90, 110, 0.7);
  border-radius: var(--radius);
  background-color: rgba(90, 90, 110, 0.05);
  "><span style="font-weight: 600; color: rgba(90, 90, 110, 0.7);">Remark.</span> The whole config lives in my <a href="https://github.com/kkanarios32/dotfiles">dotfiles</a>. It is a thin <code>init.lua</code> that loads four modules — <code>config.lazy</code>, <code>config.options</code>, <code>config.keymaps</code>, and <code>core.lsp</code> — with the actual plugin specs under <code>lua/plugins/</code> and per-server LSP tables under <code>lsp/</code>.</div>

  <section><header><h2>Sane defaults</h2></header>
<p>To my surprise, a lot of the behavior I had taken for granted turned out to be options LazyVim set internally. I was greeted on first launch by 8-space indents and a clipboard that would not survive a trip between terminal instances. So the first job was archaeology: I dug through LazyVim for every option I actually liked and copied them into <code>config/options.lua</code>. System clipboard, sensible folding via Treesitter (<code>foldexpr = v:lua.vim.treesitter.foldexpr()</code>), a global statusline, <code>conceallevel</code> for markup — the unglamorous foundation everything else sits on.</p>
</section>

  <section><header><h2>Plugin management</h2></header>
<p>The plugin manager is <code>lazy.nvim</code>, the de facto standard, and it genuinely just works. The bootstrap snippet from their docs clones it on first run, and after that every file under <code>lua/plugins/</code> that returns a spec table is imported automatically. No central manifest to keep in sync — a new file is a new plugin.</p>
</section>

  <section><header><h2>Native LSP, no framework</h2></header>
<p>This is where the config diverged most sharply from where it started. The first pass used <code>mason.nvim</code>, <code>mason-lspconfig</code>, and <code>nvim-lspconfig</code> — the standard stack. All three are now gone. Neovim 0.11 ships a <a href="https://neovim.io/doc/user/lsp/">native LSP</a> configuration API: <code>vim.lsp.config</code> defines a server and <code>vim.lsp.enable</code> turns it on, and any table returned from <code>lsp/&lt;name&gt;.lua</code> on the runtime path is picked up as that server's config. So <code>core.lsp</code> is just a list —</p>
<pre><![CDATA[vim.lsp.enable({ "forester", "texlab", "clangd", "basedpyright", "lua_ls", "ruff" })]]></pre>
<p>— and each server gets a plain Lua file in <code>lsp/</code>. No package manager, no bridge plugin translating between two config schemas. I install the servers themselves through my system package manager and let Neovim wire them up. It is dramatically less machinery for the same result, and when something breaks there is exactly one place to look.</p>
<p>The Python setup is the clearest upgrade. I dropped <code>pyright</code> for <code>basedpyright</code> and split responsibilities: <code>basedpyright</code> does type checking and inlay hints, while <code>ruff</code> runs as its own language server handling import organization and diagnostics. In the LazyVim days I fought constantly with import resolution, hand-writing a function to locate each project's virtualenv and pin <code>pythonPath</code> to its interpreter. <code>basedpyright</code> resolves environments well enough on its own that the hack is gone. <code>clangd</code>, <code>texlab</code>, and <code>lua_ls</code> round out the set, each configured in its own file — <code>texlab</code> in particular carries a fair amount of custom command wiring for building, forward search into Zathura, and environment manipulation.</p>
<p>A shared <code>LspAttach</code> autocommand in <code>core.lsp</code> binds the buffer-local LSP keymaps once, for every server: go-to-definition and friends route through <code>fzf-lua</code> pickers, and document highlighting, inlay-hint toggling, and native completion get enabled per-buffer based on what each client actually supports.</p>
</section>

  <section><header><h2>Forester finally has a <a href="https://www.forester-notes.org/MZSF/index.xml">real language server</a></h2></header>
<p>The original post described an ugly hack to get Forester completion. The <code>forester.nvim</code> plugin exposed a completion source, but only for <code>nvim-cmp</code>; <code>blink.cmp</code> (which I use — see below) needs a <code>blink.compat</code> shim, and that shim expected a plugin to <em>be</em> a source rather than to bury one inside a larger feature set. Getting reference completion working meant reading <code>blink.compat</code> internals and registering the source by hand.</p>
<p>None of that survives, because Forester now ships an actual language server. My <code>lsp/forester.lua</code> just launches <code>forester lsp</code> and hands it the forest root. The one wrinkle worth recording: <code>forester</code> locates the forest by its process working directory and <em>ignores</em> the LSP <code>rootUri</code>, and Neovim does not spawn the server in <code>root_dir</code> by default. So the config resolves the nearest <code>forest.toml</code> and starts the RPC process with an explicit <code>cwd</code> — otherwise the server exits with status 1 whenever Neovim's cwd has no <code>forest.toml</code> (e.g. when I open a tree from <code>$HOME</code>). Reference completion — indispensable for writing this very blog — now flows through the standard LSP path with no shim in sight.</p>
</section>

  <section><header><h2>Completion</h2></header>
<p>Completion is <code>blink.cmp</code>, the successor that finally displaced <code>nvim-cmp</code>. Its sources are <code>lsp</code>, <code>path</code>, <code>snippets</code>, and <code>omni</code>, with LuaSnip as the snippet engine. Because I lean on native LSP, the <code>on_attach</code> handler calls <code>vim.lsp.completion.enable</code> with autotrigger, so the server's completions feed the picker directly.</p>
<p>Layered on top is <code>supermaven-nvim</code> for AI ghost-text suggestions — accept-suggestion, accept-word, and clear bound to <code>&lt;C-l&gt;</code>, <code>&lt;C-j&gt;</code>, and <code>&lt;C-<![CDATA[]]]>&gt;</code>. It fills the role Copilot-style tools play elsewhere without getting in the way of the structured LSP and snippet completions.</p>
</section>

  <section><header><h2>Snippets</h2></header>
<p>Going all the way back to the <a href="https://castel.dev/post/lecture-notes-1/">Gilles Castel blog post</a>, I have always wanted snippets that auto-expand. Under LazyVim I had settled for friendly-snippets on native Neovim snippets; rebuilding from scratch, I refused to compromise. The engine is <code>LuaSnip</code>, which <code>blink.cmp</code> supports natively, and I have to plug the <a href="https://github.com/iurimateus/luasnip-latex-snippets.nvim">luasnip-latex-snippets repo</a> that ports the original UltiSnips math snippets over. My own snippets live in a small local plugin, <code>custom-luasnip-snippets.nvim</code>, vendored under <code>lua/local/</code>.</p>
<p>The trick I had wanted for years and can finally do: load LaTeX math snippets while inside math environments <em>in Forester</em>. The snippet plugin runs with <code>use_treesitter = true</code>, so it inspects the Forester Treesitter grammar, finds the node types for math environments, and only expands the math snippets when the cursor is actually inside one. Writing <code>\TeX </code> inside a <code>.tree</code> file feels exactly like writing it in a <code>.tex</code> file.</p>
</section>

  <section><header><h2>Formatting and linting</h2></header>
<p>Formatting runs through <code>conform.nvim</code> on save. Python is handled by <code>ruff_format</code> followed by <code>pyink</code>, Lua by <code>stylua</code> — this replaced the old <code>black</code> + <code>isort</code> pairing, folding most of it into the same <code>ruff</code> that already runs as a language server. Formatting falls back to the LSP formatter when no dedicated tool is configured for a filetype.</p>
</section>

  <section><header><h2>Fuzzy finding and forest integration</h2></header>
<p>The fuzzy finder is <code>fzf-lua</code>, which replaced Telescope. File and grep pickers are scoped to a detected project root — a small <code>utils.root</code> module (lifted from LazyVim) walks LSP workspace folders, <code>.git</code>, and cwd to find it — with a dedicated <code>tex</code> override that restricts live-grep to <code>\TeX </code> source files.</p>
<p>The piece I am most fond of is <code>forester-new</code>, a local plugin that wires the editor into the forest's own note pipeline. It shells out to <code>scripts/new</code> so every tree gets the standard frontmatter and routing, then commands like <code>:ForesterNew</code>, <code>:ForesterStub</code>, <code>:ForesterCitek</code>, and <code>:ForesterTransient</code> handle the follow-through: creating a tree and inserting a <code><![CDATA[\transclude]]></code>, stubbing a concept and dropping a <code><![CDATA[[[id]]]]></code> link at the cursor, or — in visual mode — lifting a selection into a new tree and replacing it in place with the transclude. Stub-and-link without ever leaving the sentence, which is exactly the friction the forest workflow is supposed to remove.</p>
</section>

  <section><header><h2>Anki</h2></header>
<p>I originally built a whole in-editor Anki workflow on top of <a href="https://git.sr.ht/~foosoft/anki-connectdeck-actions">AnkiConnect</a> and a vendored <code>anki.nvim</code>. That has since moved out of the editor and into the forest itself: cards are just <code><![CDATA[\taxon{Card}]]></code> trees, and <code>scripts/anki-sync</code> mirrors them into Anki — creating the note on first sync and updating in place thereafter. Writing a card is now the same as writing any other tree, snippets and all, and the sync is one command away. This is <a href="https://kellenkanarios.com/2RO9/">an example of a card</a>.</p>
</section>

  <div class="comments">
    
  <hr />

    <script src="https://utteranc.es/client.js" repo="kkanarios32/website-comments" issue-term="nvim" theme="boxy-light" crossorigin="anonymous" async="" />
  </div>
      </div>
    </content>
  </entry>
  <entry>
    <title>Stack vs. Heap</title>
    <published>2024-12-28T00:00:00Z</published>
    <updated>2024-12-28T00:00:00Z</updated>
    <author>
      <name>Kellen Kanarios</name>
      <uri>https://kellenkanarios.com/kellenkanarios/</uri>
    </author>
    <link rel="alternate" type="text/html" href="https://kellenkanarios.com/0025/" />
    <id>https://kellenkanarios.com/0025/</id>
    <content type="xhtml">
      <div xmlns="http://www.w3.org/1999/xhtml">
<p>I often hear this question come up amongst performance-driven developers, and for the longest time I could never get a straight answer as to why the stack was so much better? After a long foray into the inner workings of how memory allocation is actually done, I think I finally have an answer. I do not want to forget this, so I am doing a quick write up here.</p>
  <section><header><h2>Why stack is faster.</h2></header>
This all comes down to what happens during each memory allocation. Prior to actually running the program, the operating system allocates some amount of memory for both the stack and the heap (I think around 2-4MB?). When you allocate memory on the stack i.e. <code>int x = 1;</code>
<ul><li>Stack just increments stack pointer.</li>
    <li>Writes value to new address pointed to by stack pointer.</li></ul>
When you allocate memory on the heap i.e. <code>int *x = malloc(sizeof(int))</code>, we must instead choose how best to fulfill the request, which is a whole research direction in itself.
<ul><li>One such implementation is the <a href="https://en.wikipedia.org/wiki/Buddy_memory_allocation">buddy algorithm</a></li></ul>
However, <code>malloc</code> itself is not a system call and therefore is not terribly slow. A real issue occurs when we run out of pre-allocated heap memory. This requires <a href="https://kellenkanarios.com/A5S6/">invoking a system call</a> like <code>sbrk</code> to increment the size of the allocated memory for the heap.
<ul><li>If no available pages, OS must evict and write to disk...</li>
      <li>Formally, this is known as "heap growth".</li></ul>
Generally, if you know how much memory you need it is good practice to just allocated all up front to minimize any potential system calls. With this, the performance difference is not too drastic <![CDATA[ :)]]>
</section>

  <div class="comments">
    
  <hr />

    <script src="https://utteranc.es/client.js" repo="kkanarios32/website-comments" issue-term="pathname" theme="boxy-light" crossorigin="anonymous" async="" />
  </div>
      </div>
    </content>
  </entry>
</feed>
